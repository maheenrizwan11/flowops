DATABASE_URL="postgresql://postgres:maheen123@localhost:5432/flowops"
JWT_SECRET="replace-with-32-plus-random-chars"
JWT_EXPIRES_IN="7d"
PORT=3000
# In production, set to your deployed frontend URL (e.g. https://flowops.vercel.app).
# Leave blank in development to allow all origins.
FRONTEND_URL=
